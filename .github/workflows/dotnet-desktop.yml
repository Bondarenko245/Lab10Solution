name: .NET CI/CD Lab10

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore Lab10Solution.sln

    - name: Build project
      run: dotnet build Lab10Solution.sln --configuration Release --no-restore

    - name: Execute unit tests
      run: dotnet test Lab10Solution.sln --configuration Release --no-build --verbosity normal
